import { defineCollection } from "astro:content";

// No content collections currently used.
// Team data is managed via src/data/team.ts.
// Add collections here if needed in the future (e.g., blog, services).

export const collections = {};
